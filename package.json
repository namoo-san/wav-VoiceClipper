{
  "name": "wav-voice-clipper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "NUXT_PUBLIC_GIT_COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo 'unknown') nuxt build",
    "generate": "NUXT_PUBLIC_GIT_COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo 'unknown') nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.13.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.0",
    "@nuxt/icon": "^1.5.0",
    "typescript": "^5.3.0"
  }
}
